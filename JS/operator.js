// Logical AND
let x = 10;
let y = 20;
if (x > 0 && y < 30) {
    console.log('Both conditions are true');
}
/*
// Logical OR
let isAdmin = true;
let isLoggedIn = false;
if (isAdmin || isLoggedIn) {
    console.log('User is either an admin or logged in');
}

// Ternary Operator
let isMorning = true;
let greeting = isMorning ? 'Good Morning' : 'Good Evening';
console.log(greeting); // Output will depend on the value of isMorning
*/