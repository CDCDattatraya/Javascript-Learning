// DOM is programming interface
// In JS there is document object which includes properties and methods.
// We can capture user actions by using JS Events.

let ela = document.getElementById('adc'); // node type object

// Accessing HTML Element : DOM helps us to access HTML Element of webpage in the form of JS Object.
let elb = document.getElementsByClassName("xyz");
let elc = document.getElementsByTagName("li");
let eld = document.querySelector("xyz");  // These are methods of DOM

console.log(ela);

// Properties of DOM

