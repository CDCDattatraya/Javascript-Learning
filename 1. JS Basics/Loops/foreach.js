// foreach is not loop but used as loop.
// foreach is function which is used to iterate through arrays.

let token = [2,4,6,8];

token.forEach(function(element){
        console.log(element,"");


});