let age = 23;

//Tenary Operator is like if else condition and it takes 3 operands at a time.

//condition ? expression_A : expression_B

let result = age>18 ? "You can vote" : "You cannot vote" ;

console.log(result);